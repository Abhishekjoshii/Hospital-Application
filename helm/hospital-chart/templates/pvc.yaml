apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{.Values.persistent.pvcName}}
spec:
  accessModes:
    - ReadWriteOnce 
  resources:
    requests:
      storage: {{.Values.persistance.size}}